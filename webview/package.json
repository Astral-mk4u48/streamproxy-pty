{
  "name": "streamproxy-webview",
  "private": true,
  "version": "0.1.5",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/webview.js --format=iife --platform=browser --minify && node build-css.mjs",
    "watch": "esbuild src/index.ts --bundle --outfile=dist/webview.js --format=iife --platform=browser --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
